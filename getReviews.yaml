config:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 250
      name: "warming up"
    # - duration: 120
    #   arrivalRate: 5
    #   rampTo: 50
    #   name: "ramp up load"
    # - duration: 120
    #   arrivalRate: 50
    #   name: "sustained load"
scenarios:
  - flow:
    - get:
        url: "/reviews/{{ $randomNumber(1,1000010) }}"
    - get:
        url: "/reviews/{{ $randomNumber(1,1000010) }}/meta"