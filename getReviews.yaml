config:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 260
scenarios:
  - flow:
    - get:
        url: "/reviews/{{ $randomNumber(1,1000010) }}"
    # - get:
    #     url: "/reviews/{{ $randomNumber(1,1000010) }}/meta"